# CORS_ORIGINS=http://localhost:3000
# RUN_MIGRATIONS_ON_STARTUP=false   # Set to disable auto-migrations at startup (e.g. if you run them manually)

APP_ENV=
DB_NAME=resume_ai
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=

JWT_SECRET_KEY=dev-super-secret-key
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_MINUTES=43200

# External AI service (the `resumeai-AI` repo)
# When using Docker, set this to the AI container hostname, e.g. `http://resumeai-ai:8000`
RESUMEAI_AI_BASE_URL=http://127.0.0.1:8000
# Timeout for `/generate_resume` call
RESUMEAI_AI_TIMEOUT_SECONDS=180